// Trip Lobby: shareable link, live group voting, route reshapes on votes.
// Built out in Phase 9 (Realtime + LangGraph agent).

export default function LobbyPage() {
  return (
    <main className="flex flex-1 items-center justify-center">
      <p className="text-zinc-500">Group lobby — coming in Phase 9.</p>
    </main>
  );
}
