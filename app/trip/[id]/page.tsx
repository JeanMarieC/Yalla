// Trip view: map + hour-by-hour itinerary for a single plan.
// Built out in Phase 5 (the map centerpiece).

export default function TripPage() {
  return (
    <main className="flex flex-1 items-center justify-center">
      <p className="text-zinc-500">Trip view — coming in Phase 5.</p>
    </main>
  );
}
